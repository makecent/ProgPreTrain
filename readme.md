#
```shell
conda create -n open-mmlab
conda install pytorch torchvision torchaudio -c pytorch
pip install openmim pytorchvideo timm einops tensorboard git
mim install mmcv-full=1.5.0 mmaction2==0.24.0
```